import javax.swing.*;

public class Main {
    public static void main(String[] args) {
        //SwingUtilities.invokeLater(() -> new GameBoard(10, 10));
        //SwingUtilities.invokeLater(() -> new PlayerSave(60));
        SwingUtilities.invokeLater(() -> new MyWindow());

    }
}